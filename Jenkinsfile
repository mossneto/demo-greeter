pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            checkout scm
        }

        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
    }
}